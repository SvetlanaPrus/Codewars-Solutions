//https://www.codewars.com/kata/56ff6a70e1a63ccdfa0001b1/train/javascript


function arrayMadness(a, b) {
  return a.reduce((acc, curr) => curr**2+acc, 0) > b.reduce((acc, curr) => curr**3+acc, 0);
}
